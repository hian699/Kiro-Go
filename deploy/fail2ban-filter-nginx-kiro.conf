# /etc/fail2ban/filter.d/nginx-kiro.conf
# Lop 3 — fail2ban doc log Nginx, ban IP dinh rate-limit nhieu lan.
[Definition]
failregex = limiting requests, excess:.* by zone.*client: <HOST>
            limiting connections by zone.*client: <HOST>
ignoregex =
